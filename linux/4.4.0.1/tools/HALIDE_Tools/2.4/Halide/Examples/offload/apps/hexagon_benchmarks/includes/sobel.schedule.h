/* ==================================================================================== */
/*     Copyright (c) 2016-2021 QUALCOMM Technologies, Inc. and/or its subsidiaries.     */
/*                           All Rights Reserved.                                       */
/*                  QUALCOMM Confidential and Proprietary                               */
/* ==================================================================================== */

#ifndef sobel_SCHEDULE_H
#define sobel_SCHEDULE_H

// MACHINE GENERATED -- DO NOT EDIT
// This schedule was automatically generated by Adams2019
// for target=arm-64-android-hvx-hvx_128  // NOLINT
// with machine_params=16,24000000,40

#include "Halide.h"


inline void apply_schedule_sobel(
    ::Halide::Pipeline pipeline,
    ::Halide::Target target
) {
    using ::Halide::Func;
    using ::Halide::MemoryType;
    using ::Halide::RVar;
    using ::Halide::TailStrategy;
    using ::Halide::Var;
    Func output = pipeline.get_func(9);
    Func sobel_y = pipeline.get_func(8);
    Func sobel_y_avg = pipeline.get_func(7);
    Func sobel_x = pipeline.get_func(6);
    Func sobel_x_avg = pipeline.get_func(5);
    Func input_16 = pipeline.get_func(4);
    Func bounded_input = pipeline.get_func(3);
    Func repeat_edge = pipeline.get_func(2);
    Func lambda_0 = pipeline.get_func(1);
    Func input_im = pipeline.get_func(0);
    Var _0(repeat_edge.get_schedule().dims()[0].var);
    Var _0i("_0i");
    Var _1(repeat_edge.get_schedule().dims()[1].var);
    Var x(output.get_schedule().dims()[0].var);
    Var xi("xi");
    Var xii("xii");
    Var y(output.get_schedule().dims()[1].var);
    Var yi("yi");
    Var yii("yii");
    Var yiii("yiii");
    output
        .split(y, y, yi, 135, TailStrategy::ShiftInwards)
        .split(x, x, xi, 256, TailStrategy::ShiftInwards)
        .split(yi, yi, yii, 45, TailStrategy::ShiftInwards)
        .split(yii, yii, yiii, 3, TailStrategy::ShiftInwards)
        .split(xi, xi, xii, 128, TailStrategy::ShiftInwards)
        .unroll(xi)
        .unroll(yiii)
        .vectorize(xii)
        .align_storage(x, 128)
        .compute_root()
        .hexagon()
        .reorder({xii, xi, yiii, yii, yi, x, y})
        .parallel(y)
        .prefetch(input_im, yi, 2);
    sobel_y
        .store_in(MemoryType::Stack)
        .split(x, x, xi, 64, TailStrategy::RoundUp)
        .unroll(x)
        .vectorize(xi)
        .align_storage(x, 64)
        .compute_at(output, xi)
        .reorder({xi, x, y});
    sobel_y_avg
        .store_in(MemoryType::Stack)
        .split(x, x, xi, 64, TailStrategy::RoundUp)
        .unroll(x)
        .vectorize(xi)
        .align_storage(x, 64)
        .compute_at(output, xi)
        .reorder({xi, x, y});
    sobel_x
        .store_in(MemoryType::Stack)
        .split(x, x, xi, 64, TailStrategy::RoundUp)
        .unroll(x)
        .unroll(y)
        .vectorize(xi)
        .align_storage(x, 64)
        .compute_at(output, yii)
        .reorder({xi, x, y});
    sobel_x_avg
        .store_in(MemoryType::Stack)
        .split(x, x, xi, 64, TailStrategy::RoundUp)
        .vectorize(xi)
        .align_storage(x, 64)
        .compute_at(output, yii)
        .store_at(output, yi)
        .reorder({xi, x, y});
    input_16
        .store_in(MemoryType::Stack)
        .split(y, y, yi, 3, TailStrategy::ShiftInwards)
        .split(x, x, xi, 128, TailStrategy::ShiftInwards)
        .unroll(x)
        .unroll(yi)
        .vectorize(xi)
        .align_storage(x, 128)
        .compute_at(output, yi)
        .store_at(output, x)
        .reorder({xi, x, yi, y});
    bounded_input
        .store_in(MemoryType::Stack)
        .split(x, x, xi, 128, TailStrategy::RoundUp)
        .unroll(x)
        .unroll(y)
        .vectorize(xi)
        .align_storage(x, 128)
        .compute_at(input_16, y)
        .reorder({xi, x, y});
    repeat_edge
        .store_in(MemoryType::Stack)
        .split(_0, _0, _0i, 128, TailStrategy::ShiftInwards)
        .unroll(_0)
        .unroll(_1)
        .vectorize(_0i)
        .align_storage(_0, 128)
        .compute_at(input_16, y)
        .reorder({_0i, _0, _1});

}

#endif  // sobel_SCHEDULE_H
