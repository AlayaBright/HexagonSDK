/* ==================================================================================== */
/*     Copyright (c) 2016-2021 QUALCOMM Technologies, Inc. and/or its subsidiaries.     */
/*                           All Rights Reserved.                                       */
/*                  QUALCOMM Confidential and Proprietary                               */
/* ==================================================================================== */

#ifndef dilate3x3_SCHEDULE_H
#define dilate3x3_SCHEDULE_H

// MACHINE GENERATED -- DO NOT EDIT
// This schedule was automatically generated by Adams2019
// for target=arm-64-android-hvx-hvx_128  // NOLINT
// with machine_params=16,24000000,40

#include "Halide.h"


inline void apply_schedule_dilate3x3(
    ::Halide::Pipeline pipeline,
    ::Halide::Target target
) {
    using ::Halide::Func;
    using ::Halide::MemoryType;
    using ::Halide::RVar;
    using ::Halide::TailStrategy;
    using ::Halide::Var;
    Func output = pipeline.get_func(5);
    Func max_y = pipeline.get_func(4);
    Func bounded_input = pipeline.get_func(3);
    Func repeat_edge = pipeline.get_func(2);
    Func lambda_0 = pipeline.get_func(1);
    Func input_im = pipeline.get_func(0);
    Var x(output.get_schedule().dims()[0].var);
    Var xi("xi");
    Var xii("xii");
    Var y(output.get_schedule().dims()[1].var);
    Var yi("yi");
    Var yii("yii");
    output
        .split(x, x, xi, 1920, TailStrategy::ShiftInwards)
        .split(y, y, yi, 270, TailStrategy::ShiftInwards)
        .split(yi, yi, yii, 8, TailStrategy::ShiftInwards)
        .split(xi, xi, xii, 128, TailStrategy::ShiftInwards)
        .vectorize(xii)
        .align_storage(x, 128)
        .compute_root()
        .hexagon()
        .reorder({xii, xi, yii, yi, x, y})
        .fuse(x, y, x)
        .parallel(x)
        .prefetch(input_im, yi, 2);
    max_y
        .store_in(MemoryType::Stack)
        .split(x, x, xi, 384, TailStrategy::ShiftInwards)
        .split(xi, xi, xii, 128, TailStrategy::ShiftInwards)
        .unroll(y)
        .vectorize(xii)
        .align_storage(x, 128)
        .compute_at(output, yi)
        .reorder({xii, y, xi, x});
    bounded_input
        .store_in(MemoryType::Stack)
        .split(x, x, xi, 128, TailStrategy::GuardWithIf)
        .unroll(y)
        .vectorize(xi)
        .align_storage(x, 128)
        .compute_at(max_y, xi)
        .store_at(max_y, x)
        .reorder({xi, x, y});

}

#endif  // dilate3x3_SCHEDULE_H
