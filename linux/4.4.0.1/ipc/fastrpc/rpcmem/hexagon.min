#what we ship
BUILD_COPIES+=inc/rpcmem.h

INCDIRS += \
  inc \
  src \

BUILD_LIBS += rpcmem
rpcmem_C_SRCS = src/rpcmem_stub

BUILD_DLLS += rpcmem

BUILD_COPIES += $(LIBS) $(DLLS) $(SHIP_DIR)/ ;
